apiVersion: v1
kind: Service
metadata:
  name: frontendproxy
spec:
  type: LoadBalancer
  selector:
    app: frontendproxy
  ports:
    - port: {{ .Values.frontendproxy.service.port }}
      targetPort: {{ .Values.frontendproxy.service.port }}
